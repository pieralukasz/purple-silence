import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    width: "100%",
  },
  headerInner: {
    flexDirection: "row",
    padding: 6,
  },
  headerEmpty: {
    height: 48,
  },
});

export default styles;
