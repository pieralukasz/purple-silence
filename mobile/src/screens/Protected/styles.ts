import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  label: {
    fontSize: 12,
    lineHeight: 12,
  },
  tabStyle: {
    paddingTop: 20,
  },
});

export default styles;
